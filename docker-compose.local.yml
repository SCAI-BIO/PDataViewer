version: "3.12"

services:

  frontend:
    image: pdataviewer-frontend
    build:
      context: ./pdataviewer
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - backend

  backend:
    image: pdataviewer-backend
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:80"